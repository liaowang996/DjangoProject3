# D:\workdir\DjangoProject3\api_test_platform\api_platform\ui_auto\urls.py
from django.urls import path

# 必须定义命名空间，与应用名一致（避免路由冲突）
app_name = 'ui_auto'

# 待开发：UI自动化相关路由（当前为空，仅保证模块可导入）
urlpatterns = [
    # 示例：未来可添加UI自动化路由，如
    # path('run-test/', views.run_ui_test, name='run_ui_test'),
]